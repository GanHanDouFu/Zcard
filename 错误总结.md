# Zcard 卡片不显示问题总结

## 现象

生成知识卡片后，控制台已经显示卡片数量增加：

```text
[Zcard] API 调用成功
[Zcard] renderCards: 过滤后卡片数量: 4
```

但页面上看不到新增卡片，用户会误以为“卡片没有生成”或“页面没有加载出来”。

腾讯云静态部署后，还出现过页面只有原生 HTML 默认样式的情况，按钮、输入框和标题都没有正常 UI。

## 根因一：复习滑动卡片和首页卡片 class 撞名

新增“今日复习左右滑动”功能后，复习弹窗里的卡片使用了 `.swipe-card`：

```html
<div class="swipe-card" id="swipe-review-card">
```

但首页卡片列表也使用了同一个 class：

```html
<div class="knowledge-card swipe-card">
```

复习功能新增的 CSS 写成了全局选择器：

```css
.swipe-card {
    position: absolute;
    inset: 0;
    ...
}
```

这导致首页列表里的普通卡片也被强制变成 `position: absolute`，布局高度被破坏。卡片数据和 DOM 都存在，但视觉上被压没了，所以看起来像“没有显示”。

### 修复方式

把复习弹窗的样式限定在复习场景内：

```css
.swipe-scene > .swipe-card {
    position: absolute;
    inset: 0;
    ...
}
```

同时把左右滑动状态样式也收窄：

```css
.swipe-scene > .swipe-card.swiping-left { ... }
.swipe-scene > .swipe-card.swiping-right { ... }
```

这样首页卡片继续使用原本的滑动删除样式，复习卡片使用独立的弹窗样式，互不污染。

## 根因二：移动端滚动容器不是 window

手机布局里，真正滚动的容器是 `.main-content`，不是整个 `window`。

生成卡片后虽然调用了渲染，但用户仍停留在输入框和搜索框附近，看不到下面新生成的卡片。

### 修复方式

给每张卡片加上 `data-card-id`：

```js
cardEl.dataset.cardId = card.id;
```

生成成功后，主动找到新卡片并滚动 `.main-content`：

```js
focusGeneratedCard(newCard.id);
```

并给新卡片短暂加高亮状态：

```css
.knowledge-card-wrap.is-new .knowledge-card {
    animation: newCardPulse 1.6s ease;
}
```

这样用户能明确看到新卡片已经生成。

## 根因三：腾讯云静态部署路径导致 CSS/JS 加载失败

腾讯云访问地址如果是：

```text
https://xxx.tcloudbaseapp.com/Zcard-5-10
```

没有最后的 `/`，浏览器会把 `Zcard-5-10` 当成一个文件路径。

此时页面里的相对资源：

```html
<link rel="stylesheet" href="style.css?v=...">
<script src="script.js?v=..."></script>
```

会被解析成：

```text
https://xxx.tcloudbaseapp.com/style.css
https://xxx.tcloudbaseapp.com/script.js
```

而不是：

```text
https://xxx.tcloudbaseapp.com/Zcard-5-10/style.css
https://xxx.tcloudbaseapp.com/Zcard-5-10/script.js
```

所以 CSS/JS 加载失败，页面就会变成浏览器默认样式。

### 解决方式

推荐把以下文件直接上传到腾讯云静态网站根目录：

```text
index.html
style.css
script.js
```

然后访问根地址：

```text
https://xxx.tcloudbaseapp.com/
```

如果必须放在 `/Zcard-5-10/` 子目录，则访问地址必须带最后的 `/`：

```text
https://xxx.tcloudbaseapp.com/Zcard-5-10/
```

## 定位思路

这次问题的关键判断是：

```text
renderCards: 过滤后卡片数量: 4
```

这说明卡片数据已经生成，渲染函数也已经执行。问题不在 API，也不在保存逻辑，而在“渲染后的视觉呈现”。

后续通过检查 CSS 发现两个 `.swipe-card` 定义：

```text
style.css:593  .swipe-card
style.css:1390 .swipe-card
```

第二个定义来自复习滑动功能，并且覆盖了首页卡片样式，最终确认是 class 冲突。

## 经验教训

1. 新增功能时不要复用过于通用的 class 名，例如 `.card`、`.swipe-card`、`.modal-card`。
2. 弹窗或局部功能的样式要加作用域，例如 `.swipe-scene > .swipe-card`。
3. 控制台显示数据数量增加时，要优先怀疑 CSS 布局、筛选状态、滚动位置，而不是 API。
4. 移动端页面如果使用内部滚动容器，生成新内容后要滚动对应容器，而不是默认依赖 `window.scrollTo` 或 `scrollIntoView`。
5. 静态部署到子目录时，要特别注意相对路径和访问地址最后的 `/`。

## 当前修复结果

已修复：

- 首页卡片和复习卡片的 `.swipe-card` 样式冲突
- 生成后新卡片自动滚动到可见区域
- 新卡片短暂高亮
- 更新静态资源版本号，减少浏览器缓存旧 CSS/JS 的概率

已验证：

```text
cardCount: 4
domCards: 4
wrapHeight: 202
cardPosition: relative
visible: true
```

说明卡片已经正常渲染并可见。
